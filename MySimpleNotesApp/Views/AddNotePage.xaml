<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Shell.NavBarIsVisible="False"
             x:Class="MySimpleNotesApp.Views.AddNotePage"
             Title="AddNotePage">

    <Grid RowDefinitions="Auto,* ,Auto" Padding="20" RowSpacing="15">

        <Entry Grid.Row="0" Placeholder="Tytuł" Text="{Binding Title}" />

        <Editor Grid.Row="1" Text="{Binding Content}" AutoSize="Disabled" VerticalOptions="Fill" HorizontalOptions="Fill" BackgroundColor="LightYellow" TextColor="Black"/>

        <Button Grid.Row="2" Text="Zapisz" Command="{Binding SaveCommand}" />

    </Grid>



</ContentPage>